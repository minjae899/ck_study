<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User">

	<select id="selectLoginInfo" resultType="memberVO">
		SELECT SEQ
		     , NAME
		     , ID
		     , PW
		     , AGE
		     , GENDER
		     , TELL
		     , REGISTER_DATE
		     , UPDATE_DATE
		  FROM MEMBER
		 WHERE ID = #{id}
		   AND PW = #{pw}
	</select>
	
</mapper>